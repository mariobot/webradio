@model List<WebRadio.Models.Languages>

<h2 class="text-center">Languages</h2>

@if (Model.Count > 0)
{
    <div class="row text-center">
        @foreach (var item in Model)
        {
            <a href="@Url.Action("ByLanguage",new { id=item.value })">@item.name (@item.stationcount)</a>
        }
    </div>
}