@model List<WebRadio.Models.Tags>



<h2>Tags</h2>


@if(Model.Count > 0)
{
    <div class="rowstyle">
        @foreach (var item in Model)
        {
            <a href="@Url.Action("ByTag",new { id=item.value })">@item.name</a>
        }
    </div>
}
