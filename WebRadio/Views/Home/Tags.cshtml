@model List<WebRadio.Models.Tags>



<h2 class="text-center">Tags</h2>


@if(Model.Count > 0)
{
    <div class="text-center">
        @foreach (var item in Model)
        {
            <a href="@Url.Action("ByTag",new { id=item.value })">@item.name (@item.stationcount)</a>
        }
    </div>
}
